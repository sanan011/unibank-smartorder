# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=order_db
DB_USER=smartorder
DB_PASSWORD=supersecret_db_password

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=supersecret_redis_password

# RabbitMQ Configuration
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=supersecret_rmq_password

# JWT Security
# MUST BE AT LEAST 512 BITS (64 CHARACTERS) LONG!
JWT_SECRET=this-is-a-very-secret-key-that-must-be-at-least-512-bits-long-for-hmac-sha512-algorithm
